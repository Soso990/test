---
apiVersion: apps/v1 #eesdfwrghsdsfsdfsfsdsdfswef
kind: Deployment
metadata:
  name: nginxtestjenkins
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: nginx
  template: 
    metadata:
      labels:
        app: nginxtestjenkins
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 8081
---
apiVersion: v1
kind: Service
metadata:
  name: nginx
  namespace: default
  labels:
    app: nginxtestjenkins
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  externalTrafficPolicy: Local
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: 8082
  selector:
    app: nginxtestjenkins
  type: LoadBalancer
